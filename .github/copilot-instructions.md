# Copilot Instructions

## Project Guidelines
- User is a backend developer and prefers using DI with an ILlmClient interface and separate implementations per provider, configured from JSON.
- If the user specifies particular lines of an issue, make corrections exactly at the indicated location without altering related but non-problematic logic.